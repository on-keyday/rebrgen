
format Label:
    length :u8
    if length & 0xc0 == 0xc0:
        input.backward()
        pointer :u16
    else:
        length <= 63
        data :[length]u8

format Test:
    label :Label
    available(label.pointer) == true
